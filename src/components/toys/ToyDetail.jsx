import React from 'react';
import useTitle from '../../hooks/useTitle';

const ToyDetail = () => {
    useTitle('ToyDetail')
    return (
        <div>
            <p>ToyDetail compo</p>
        </div>
    );
};

export default ToyDetail;